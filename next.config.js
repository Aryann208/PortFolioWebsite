/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'https://portfolio-website-sooty-alpha.vercel.app/',
    ]
  },
};

module.exports = nextConfig;
